#include<bits/stdc++.h>
using namespace std;
#define ll long long 
int main(){
    int t;
    cin>>t;
    while(t--){
        ll a,b;
        cin>>a>>b;
        ll x=sqrt(a);
        x=min(x,b);
        ll ans=0;
        ans+=(x-1)*(x)/2;
        x++;
        while(x<=min(a,b)){
            ans+=
        }
    }
}