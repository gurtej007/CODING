#include<bits/stdc++.h>
using namespace std;
int solve(vector<int> &arr,int w){
    if(w==1){
        return arr.size();
    }

    
}
int main(){
    int t;
    cin>>t;
    while(t--){
        int w,n;

        cin>>w>>n;

        vector<int> arr(n);

        for(int i=0;i<n;i++){
            cin>>arr[i];
        }
        sort(arr.begin(), arr.end());
        void solve(arr,w);
    }
}