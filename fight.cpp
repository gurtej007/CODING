#

int main(){
    int t;
    cin>>t;

    while(t--){
        int n;
        cin>>n;
        int q;
        cin>>q;
        vector<int> arr(n);

        for(int i=0;i<n;i++){
            cin>>arr[i];
        }
        while(q--){
            int fn;
            cin>>fn;
            
            int r;
            cin>>
        }
        int ans=0;
        solve(arr,n,x,ans);
        cout<<ans<<"\n";
    }
}